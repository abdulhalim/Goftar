# گفتار — راهنمای نصب

<p align="center">
  <strong>راهنمای گام‌به‌گام نصب گفتار روی سرور وب شما</strong>
</p>

<p align="center">
  <img src="https://github.com/abdulhalim/Goftar/raw/main/screenshots/fa-screenshot1.png" alt="صفحه اصلی گفتار" width="600"/>
</p>

---

## پیش‌نیازها

قبل از شروع، مطمئن شوید محیط هاست شما نیازمندی‌های زیر را دارد:

- **PHP نسخه 7.4 یا بالاتر** (نسخه 8.1+ توصیه می‌شود)
- افزونه‌های PHP مورد نیاز: `sqlite3`، `gd`، `mbstring`
- افزونه‌های PHP توصیه‌شده: `openssl`، `curl`
- سرور وب: Apache با `mod_rewrite` فعال، یا Nginx
- حدود 20 مگابایت فضای خالی

> گفتار روی **هاست اشتراکی** هم عالی کار می‌کند! بدون MySQL، بدون Composer، بدون تنظیمات خاص سرور.

---

## مرحله ۱ — دانلود آخرین نسخه

به صفحه [ریلیزهای گیت‌هاب گفتار](https://github.com/abdulhalim/Goftar/releases) بروید و آخرین فایل zip را دانلود کنید (مثلاً `Goftar_1.0.2-Beta.zip`).

همچنین می‌توانید از طریق Git مخزن را کلون کنید:

```bash
git clone https://github.com/abdulhalim/Goftar.git
```

---

## مرحله ۲ — آپلود فایل‌ها روی سرور

فایل zip دانلود‌شده را روی کامپیوتر خود Extract کنید، سپس **تمام فایل‌ها و پوشه‌ها** را در مسیر روت هاست خود آپلود کنید.

مسیرهای رایج روت هاست:

| نوع هاست | مسیر روت |
|---------|---------|
| cPanel | `public_html/` |
| Plesk | `httpdocs/` |
| DirectAdmin | `public_html/` |
| VPS / سرور اختصاصی | `/var/www/html/` |
| محلی (XAMPP) | `htdocs/` |

روش‌های آپلود:

- **فایل منیجر cPanel** — فایل zip را آپلود و Extract کنید
- **FTP** (FileZilla، WinSCP) — فایل‌های Extract شده را آپلود کنید
- **SSH / SCP** — برای VPS یا سرور اختصاصی

مثال با SSH:

```bash
# Extract کردن zip
unzip Goftar_1.0.2-Beta.zip

# آپلود به سرور
scp -r Goftar_1.0.2/* user@your-server:/var/www/html/
```

ساختار پوشه‌های شما باید چیزی شبیه این باشد:

```
your-site/
├── admin/           # پنل مدیریت
├── api/             # API
├── assets/          # فایل‌های فرانت‌اند
├── functions/       # توابع اصلی
├── handlers/        # پردازشگر فرم‌ها
├── includes/       # اجزای مشترک
├── install/        # نصب‌کننده
├── lang/           # فایل‌های زبان
├── public/         # صفحات فرانت‌اند
├── theme/          # سیستم تم
├── vendor/         # کتابخانه‌های شخص ثالث
├── config/         # پیکربندی (نیاز به نوشتن)
├── storage/        # دیتابیس و آپلودها (نیاز به نوشتن)
├── index.php       # نقطه ورود اصلی
└── robots.php      # تولید Robots.txt
```

---

## مرحله ۳ — تنظیم دسترسی فایل‌ها

سرور وب برای پوشه‌های `storage/` و `config/` نیاز به دسترسی نوشتن دارد.

### روی Linux / VPS / SSH:

```bash
# رفتن به مسیر سایت
cd /var/www/html/

# تنظیم دسترسی‌ها
chmod -R 755 storage/
chmod -R 755 config/
```

### روی cPanel / هاست اشتراکی:

1. **فایل منیجر** را باز کنید
2. روی پوشه `storage` کلیک راست کنید -> **Permissions** -> عدد **755** را تنظیم کنید
3. روی پوشه `config` کلیک راست کنید -> **Permissions** -> عدد **755** را تنظیم کنید

### روی DirectAdmin:

1. **فایل منیجر** را باز کنید
2. پوشه‌های `storage` و `config` را انتخاب کنید
3. دسترسی را روی **755** تنظیم کنید

> اگر مشکل دسترسی باقی ماند، عدد **777** را امتحان کنید (ایمنی کمتر اما روی بعضی هاست‌های اشتراکی لازم است).

---

## مرحله ۴ — اجرای نصب‌کننده وب

1. مرورگر وب خود را باز کنید
2. آدرس سایت خود را وارد کنید (مثلاً `https://yourdomain.com/`)
3. به‌طور خودکار به صفحه نصب‌کننده در مسیر `/install/` هدایت می‌شوید

نصب‌کننده دارای **۵ مرحله** است:

### ۴.۱ — بررسی نیازمندی‌های سرور

نصب‌کننده به‌طور خودکار بررسی می‌کند:
- نسخه PHP (باید 7.4+ باشد)
- افزونه‌های مورد نیاز PHP (`sqlite3`، `gd`، `mbstring`)
- قابلیت نوشتن در پوشه‌ها

اگر مشکلی وجود دارد، آن را رفع کنید و روی **بررسی مجدد** کلیک کنید.

### ۴.۲ — پیکربندی سایت

اطلاعات زیر را وارد کنید:
- **عنوان سایت** — نام انجمن شما (مثلاً «جامعه من»)
- **شعار سایت** — توضیح کوتاه (اختیاری)
- **آدرس سایت** — آدرس کامل سایت (به‌طور خودکار تشخیص داده می‌شود)
- **زبان پیش‌فرض** — فارسی یا English
- **تم پیش‌فرض** — تاریک یا روشن

### ۴.۳ — حساب مدیر

حساب مدیر خود را بسازید:
- **نام کاربری** — نام ورود مدیر
- **نام نمایشی** — نامی که در انجمن نمایش داده می‌شود
- **رمز عبور** — رمز عبور قوی
- **تأیید رمز عبور** — مجدداً رمز عبور را وارد کنید

### ۴.۴ — سؤالات امنیتی

**۳ سؤال امنیتی** برای بازیابی رمز عبور تنظیم کنید:
- برای هر سؤال، یکی را انتخاب و جواب آن را وارد کنید
- این سؤالات زمانی استفاده می‌شوند که رمز عبور خود را فراموش کنید

### ۴.۵ — بررسی و نصب

تمام تنظیمات را در صفحه خلاصه بررسی کنید. اگر همه چیز درست بود، روی **نصب** کلیک کنید.

نصب‌کننده موارد زیر را انجام می‌دهد:
- دیتابیس SQLite را می‌سازد
- تمام جداول لازم را ایجاد می‌کند
- فایل‌های پیکربندی را ذخیره می‌کند
- فایل `installed.lock` را ایجاد می‌کند
- به‌طور خودکار **خود را غیرفعال می‌کند**

---

## مرحله ۵ — بعد از نصب

پس از اتمام نصب‌کننده:

1. روی **رفتن به انجمن** کلیک کنید یا آدرس سایت را باز کنید
2. با اطلاعات مدیر وارد شوید
3. تموم شد! انجمن شما الان آماده کار است

> پوشه `/install/` پس از نصب به‌طور خودکار غیرفعال می‌شود. می‌توانید با خیال راحت آن را روی سرور نگه دارید.

---

## ساخت اولین دسته‌بندی

1. با حساب مدیر وارد انجمن شوید
2. روی **نام کاربری** خود در گوشه بالا-راست صفحه کلیک کنید
3. **پنل مدیریت** را از منوی کشویی انتخاب کنید
4. در نوار کناری چپ، روی **مدیریت دسته‌بندی‌ها** کلیک کنید
5. روی دکمه **افزودن دسته‌بندی جدید** کلیک کنید
6. اطلاعات زیر را وارد کنید:
   - **نام دسته‌بندی** (مثلاً «گفتگوی آزاد»)
   - **توضیحات** (توضیح کوتاه درباره دسته‌بندی)
   - **آیکون** (یک آیکون Font Awesome انتخاب کنید)
   - **رنگ** (یک رنگ اکسانت برای دسته‌بندی انتخاب کنید)
7. روی **ذخیره** کلیک کنید

دسته‌بندی شما الان روی صفحه اصلی انجمن نمایش داده می‌شود.

---

## ساخت اولین موضوع (پست)

1. به صفحه اصلی انجمن بروید
2. روی دسته‌بندی‌ای که ساختید کلیک کنید
3. روی دکمه **موضوع جدید** کلیک کنید (معمولاً بالا-راست صفحه)
4. اطلاعات زیر را وارد کنید:
   - **عنوان** — موضوع بحث شما
   - **محتوا** — پست خود را با ویرایشگر Markdown بنویسید
   - می‌توانید متن را قالب‌بندی کنید، تصویر اضافه کنید، فایل پیوست کنید، نظرسنجی بسازید و...
5. روی **ارسال** کلیک کنید

اولین موضوع شما الان زنده است! کاربران می‌توانند آن را ببینند و پاسخ دهند.

---

## عیب‌یابی

### صفحه نصب‌کننده نمایش داده نمی‌شود یا صفحه خالی است
- مطمئن شوید `index.php` در مسیر درست قرار دارد
- بررسی کنید PHP کار می‌کند: یک فایل `phpinfo.php` با محتوای `<?php phpinfo(); ?>` بسازید و آن را باز کنید

### خطای «Permission denied»
- دوباره بررسی کنید `storage/` و `config/` دسترسی **755** دارند
- روی بعضی هاست‌های اشتراکی ممکن است به **777** نیاز باشد

### خطای «Database error» بعد از نصب
- مطمئن شوید پوشه `storage/` قابلیت نوشتن دارد
- بررسی کنید افزونه `sqlite3` PHP فعال است

### صفحه نصب‌کننده پیام «قبلاً نصب شده» نشان می‌دهد
- فایل `storage/config/installed.lock` را حذف کنید و دوباره `/install/` را باز کنید
- **هشدار:** این کار دیتابیس شما را ریست می‌کند!

---

## نیاز به کمک دارید؟

- 🐙 [ثبت مشکل در گیت‌هاب](https://github.com/abdulhalim/Goftar/issues)
- 🌐 وب‌سایت گفتار: [https://goftarforum.ir](https://goftarforum.ir)

---

## لینک‌ها

| منبع | لینک |
|-----|------|
| مخزن گیت‌هاب | [https://github.com/abdulhalim/Goftar](https://github.com/abdulhalim/Goftar) |
| ریلیزها | [https://github.com/abdulhalim/Goftar/releases](https://github.com/abdulhalim/Goftar/releases) |
| وب‌سایت | [https://goftarforum.ir](https://goftarforum.ir) |
| مجوز | MIT |
